# iot-smart-greenhouse-project
IoT-based smart greenhouse system using data analytics and machine learning for crop growth prediction, developed as part of a research project.


## Trained Model File
The trained Random Forest model (`rf_model.pkl`) is not included in this repository due to GitHub file size limitations.
The model can be regenerated by running the training notebook in the `training/` folder.

